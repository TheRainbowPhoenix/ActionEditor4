# Aquedi engine notes - 2026-08-29

## Tool access

- IDA MCP works against the loaded `Game_v1020.exe` database.
- Frida is present:
  - workspace venv: `.\venv\Scripts\frida.exe` version `17.3.2`
  - embedded python env: `.\python\Scripts\frida.exe` version `17.3.2`
- MSVC Toolkit 2003 compiler exists at `C:\Program Files (x86)\Microsoft Visual C++ Toolkit 2003\bin\cl.exe`.
- `work_aquedi/cpp_experiments/winmain_probe.cpp` is now a D3D9 WinMain probe. It compiles with the Toolkit compiler when explicitly given Toolkit include/lib paths plus Windows 8.1 SDK include/lib paths. The compiler emits many expected warnings because the Windows 8.1 headers are newer than VC7.1, but it produced `work_aquedi/cpp_experiments/winmain_probe.exe`.
- `work_aquedi/cpp_experiments/build_winmain_probe.bat` recompiles it on demand.
- Import check confirms `d3d9.dll` / `Direct3DCreate9` are imported.
- Runtime check confirmed D3D9 HAL/SWVP device creation:
  - `GetAdapterDisplayMode hr=0x00000000 fmt=0x00000016`
  - `CreateDevice HAL/SWVP hr=0x00000000`
- `work_aquedi/frida/smoke_scene_physics.js` launches under Frida with this CLI form:
  - `.\venv\Scripts\frida.exe -q -t 10 --kill-on-exit -l work_aquedi\frida\smoke_scene_physics.js -f Game_v1020.exe`
- This Frida version resumes spawned targets by default. The old `--no-pause` flag is rejected; `--pause` is now the optional inverse.

## Clean workspace

- C++ experiments: `work_aquedi/cpp_experiments`
- Frida scripts/logs: `work_aquedi/frida`
- Notes: `work_aquedi/notes`
- Tool outputs: `work_aquedi/outputs`

## Confirmed IDA anchors

- `0x402830` `StageScene_Init_LoadStageFile`
- `0x406FA0` `StageScene_LoadStage`
- `0x402D50` `StageScene_State1_Playing`
- `0x4063A0` `StageScene_Render`
- `0x452210` `scene_manager`
- `0x44D910` `Scene_Update`
- `0x450070` `WorldMapScene_Update`
- `0x442990` `MenuScene_TitleScreen_BuildContent`
- `0x44E090` `MenuScene_DrawFrame_StageSelect`
- `0x44E2E0` `MenuScene_DrawFrame_FileSelect`

## Stage loading

`StageScene_Init_LoadStageFile` initializes stage/render resources and calls:

- `StageScene_LoadStage(this, 1, 1)` when `dword_5A0BE8 == 6`
- otherwise `StageScene_LoadStage(this, 0, 1)`

The prior notes are useful as leads, but some names and comments are guessed. Treat the exe, IDA decompile/disasm, and Frida traces as source of truth.

## Physics update order, player path

Confirmed in `StageScene_State1_Playing` around `0x4033F0..0x4037CE`:

1. `Entity_ProcessActions(player, 0)`
2. vtable `+76` status/death check
3. tile effects
4. optional `Entity_SyncWithParent` when `entity+812 != 0`
5. vtable `+68`: horizontal movement/update
6. `Entity_ClampToScreenBounds`
7. `Entity_ResolveEnvironmentCollisions`
8. `Entity_ClampToScreenBounds`
9. `Entity_ApplyGravity`
10. vtable `+72`: vertical movement/update
11. `Entity_ClampToScreenBoundsY`
12. `Entity_ResolveFloorAndCeilingCollision`
13. `Entity_ClampToScreenBoundsY`
14. restore/rollback checks for opposing contacts
15. `Entity_CheckAllCollisions`
16. `Entity_HandleCollision`
17. `Entity_ResolveFloorCeilingSecondPass`
18. final restore/rollback checks
19. `Entity_ApplyFrictionX`
20. `Entity_ApplyFrictionX2`

The old `validate_v7.js` header says gravity is step 2. That header is stale for the player path.

## Physics update order, actors

Actors use the same broad sequence inside the substep loop:

- process actions
- vtable `+68`
- `Entity_ResolveEnvironmentCollisions`
- `Entity_ApplyGravity`
- vtable `+72`
- `Entity_ResolveFloorAndCeilingCollision`
- `Entity_CheckAllCollisions`
- `Entity_HandleCollision`
- `Entity_ResolveFloorCeilingSecondPass`
- friction if no fatal/status transition

## Collision leaf facts

- `Entity_ApplyGravity` at `0x4698B0` adds global `g_gravity_constant` to `entity+184` only when:
  - `entity+252 == 0`
  - `entity+812 == 0`
  - `entity+680 == 0`
  - `*(double *)(entity+656) >= 0.0`
- `Entity_ResolveEnvironmentCollisions` at `0x40CE90` checks wall order based on `entity+176`:
  - `vel_x > 0`: right wall first, then left wall
  - `vel_x <= 0`: left wall first, then right wall
- `0x40D190` checks `xmax` and adjusts `xmin` left. This is the right-wall resolver.
- `0x40CFA0` checks `xmin` and adjusts `xmin` right. Its current guessed name is misleading.
- `Entity_ResolveFloorAndCeilingCollision` at `0x40D390` clears `entity+712`, then:
  - `vel_y > 0`: floor first, then ceiling
  - `vel_y <= 0`: ceiling first, then floor
- `Entity_CheckFloorTile` at `0x40D740` checks bottom edge `(xmin, ymax) -> (xmax, ymax)`.
- `Entity_CheckCeilingTile` at `0x40D670` checks top edge `(xmin, ymin) -> (xmax, ymin)`.
- `Entity_OnLand` at `0x469930` zeros vertical velocity fields, sets/clears floor/air flags, and snaps by subtracting `((int)ymax & 31) + 1` from `ymin`, then recomputes bounds through vtable `+64`.

## Tile collision rule

`GetTileCollisionAttribute` at `0x45E4F0`:

- returns `0` when tile animation/trigger conditions hide the tile
- returns `0` when `tile+107 == entity+120`
- otherwise returns `*(uint8_t *)(tile+106)`

So tile collision byte is `TileEntry+106`, category/ignore group is `TileEntry+107`.

`World_CheckTileCollision` at `0x40CB10` converts world coordinates using:

- `tile_x = x >> 5`
- `tile_y = y >> 5`
- `tile_idx = tile_x + (tile_y << scene[27])`

It samples `entity+475 + 1` points along the queried edge and returns the tile index with the highest collision attribute, using center proximity as tie-break for horizontal scans.

## Immediate next hook target

The smoke run reached `scene_manager` in the title/menu loop but did not enter StageScene because no input was replayed. Next practical step is a controlled Frida session that either records user keystrokes while manually navigating to gameplay, or hooks `Input_UpdateStateBuffer` / `Input_GetVectorFromKeys` and injects a deterministic menu navigation sequence.

## Frida menu navigation trace

`work_aquedi/frida/nav_scene_physics.js` now automates enough menu input through `GetAsyncKeyState` to reach gameplay:

- scene transitions observed: `0 -> 1 -> 6`
- `StageScene::ctor` is reached
- `StageScene_Init_LoadStageFile` is reached
- `StageScene_LoadStage(this, 0, 1)` is reached
- the player vector is valid after load; in this runtime layout, the useful vector pointers are at `g_player_entity+4` and `g_player_entity+8`, while `g_player_entity+0` is zero

The script disables synthetic menu input at `StageScene_LoadStage` entry and clears gameplay input masks after the first gameplay frame. A rerun still showed player `vx=0.400` with `dirMask=0` and `btnMask=0`, so the first-stage horizontal movement is not stale key state.

## Runtime physics facts from navigation trace

- `g_gravity` / `dbl_5A9818` is `0.004000` at runtime.
- `g_physics_substeps` is `10`.
- `g_max_air_time` is `80`.
- Initial player bounds after load:
  - `x=[392.50,407.50]`
  - `y=[744.00,767.00]`
  - `v=(0.000,0.000)`
  - `ground=0xFFFFFFFF`
  - `inAir=1`
  - `airT=0`
- Player vtable discovered from runtime entity:
  - vtable pointer: `0x56B7B4`
  - vtable `+68`: `0x4A1200`, horizontal movement/update
  - vtable `+72`: `0x4A13B0`, vertical movement/update
- `Entity_ProcessActions(player, 0)` did not mutate player position or velocity in the idle sample.
- `vtable+68` moved `x` by `vx` each substep. Example: `x=[392.50,407.50]`, `v=(0.400,0.000)` on entry; `x=[392.90,407.90]` on leave.
- `Entity_ApplyGravity` adds `0.004` to `vy` per substep.
- `vtable+72` moves `y` by current `vy` each substep. Example: after gravity `vy=0.008`; `vtable+72` moved `y` from `744.00` to `744.01`.

Current working hypothesis: the stage/player spawn or default action state initializes/maintains `vx=0.400`; this should be chased through `0x4A1200` and the action/state fields it reads, not through input.

## Player `vtable+68` / `0x4A1200`

Runtime player vtable `+68` is `0x4A1200`. It is the X movement/update method:

- if `entity+0x288` / `double[81]` is nonzero, it copies that impact/speed override into `entity+0xB0` (`speed_x`) and logs `MoveX()`
- otherwise, if `entity+0xC0` is nonzero, it decays that field through `sub_49E920`
- then it adds `entity+0xC0` into `entity+0xB0`
- it clamps `entity+0xB0` to `+-dbl_5A9820`
- it adds `entity+0xB0` into `entity+0x80` (`x_min`)
- it calls vtable `+0x3C` to refresh derived bounds

Important field observations:

- `entity+0x80`: `x_min`
- `entity+0x88`: `y_min`
- `entity+0x90`: `x_max`
- `entity+0x98`: `y_max`
- `entity+0xB0`: `speed_x`
- `entity+0xB8`: `speed_y`
- `entity+0xC0`: X acceleration/impact accumulator used by `0x4A1200`
- `entity+0xC8`: Y acceleration/impact accumulator used by `0x4A13B0`
- `entity+0xD0`: decay amount used by `sub_49E920`
- `entity+0x288`: X impact override
- `entity+0x290`: Y impact override

`0x4A13B0` is the paired Y movement/update method:

- calls `sub_46A3B0` first to fold Y impact into `speed_y`
- applies `entity+0xC8` when enabled
- calls `sub_49E820`, which clamps `speed_y`, adds it into `y_min`, then calls collision-ish `sub_49E1E0`
- refreshes bounds via vtable `+0x40`

`sub_46B130(input)` does not read only the two visible input masks. It scans ten per-direction/per-button state records at `input+0x210`, populated by `Input_UpdateHoldCounters` and `Input_UpdateStateBuffer`. Replay must inject masks before `Input_UpdateHoldCounters`; injecting only after `Input_UpdateStateBuffer` leaves those derived records stale and does not move the player.

## Input record/replay loop

Added:

- `work_aquedi/frida/record_gameplay_inputs.js`
- `work_aquedi/frida/replay_latest_inputs.js`

Recorder behavior:

- waits for first `scene_id=0`, then sends menu navigation via both `GetAsyncKeyState` override and `keybd_event`/`PostMessageA`
- stops synthetic menu input at `StageScene_LoadStage`
- writes JSONL to `work_aquedi/outputs/input_record_latest.jsonl`
- records every gameplay frame's accepted `dir` and `btn` masks plus player state

Current saved sample:

- `work_aquedi/outputs/input_record_20260829_191903.jsonl`
- `work_aquedi/outputs/input_record_latest.jsonl`

Replay behavior:

- reads `input_record_latest.jsonl`
- uses scene-anchored menu navigation
- injects recorded `dir`/`btn` before `Input_UpdateHoldCounters`
- verified reaching `scene_id=6`
- verified replaying movement: frame 30 had `dir=2`, `x0=452.5`, `vx=0.4`; jumps with `btn=2` reproduce vertical launch behavior

Direct replay caveat: the current replay is close enough for engine-path validation, but it is not guaranteed bit-identical over long windows because small frame/start timing differences can change collisions and landing frames.

## First-pass entity/motion struct map

`struct_this_move` and `struct_this_movey` are partial IDA views of the same entity object. The object is not just a movement component; it carries render bounds, motion, collision shape data, action vectors, contact flags, and input-derived command state.

Core position and motion:

- `+0x000`: vtable
- `+0x080`: `x_min` / world X left
- `+0x088`: `y_min` / world Y top
- `+0x090`: `x_max` / world X right
- `+0x098`: `y_max` / world Y bottom
- `+0x0B0`: `speed_x`
- `+0x0B8`: `speed_y`
- `+0x0C0`: X accel/impulse accumulator, decayed by `sub_49E920`
- `+0x0C8`: Y accel/impulse accumulator
- `+0x0D0`: decay amount used by `sub_49E920`
- `+0x0E0`: enable decay for `+0x0C0`
- `+0x0E4`: enable decay for `+0x0C8`
- `+0x0F0`: object height used by `sub_49E1E0` world-bound clamp
- `+0x0FC`: axis/form mode; `0` normal/horizontal, `1+` alternate axis behavior
- `+0x100`: axis subtype byte used with `+0x0FC`
- `+0x288`: `impact_x`; if nonzero, `MoveX` copies this directly into `speed_x`
- `+0x290`: `impact_y`; folded into `speed_y` by `sub_46A3B0`

Identity/input/action:

- `+0x070`: player-controlled flag; if set, command code uses input device 0
- `+0x078`: tile collision ignore/category id passed to `GetTileCollisionAttribute`
- `+0x1D3`: input device index for non-player entities
- `+0x1D6`: entity form/type byte updated by `0x4A14B0`
- `+0x1DB`: collision edge sample count minus one; `World_CheckTileCollision` samples `+1`
- `+0x21C`: active command/action vector header; entries appear to be 136 bytes
- `+0x220/+0x224`: active command/action vector begin/end, depending on IDA type view

Screen/sprite/collision dimensions:

- `+0x03C..+0x048`: screen-space AABB from `Entity_UpdateBoundingBox`
- `+0x04C`: sprite/draw offset X
- `+0x050`: sprite/draw offset Y
- `+0x054`: sprite width
- `+0x058`: sprite height
- `+0x0EC/+0x0F0`: tile sample step X/Y copies used by edge sampling
- `+0x470/+0x474/+0x478/+0x47C`: main collision box offset X/Y and width/height
- `+0x480/+0x484/+0x488/+0x48C`: second copy of main collision box offset X/Y and width/height
- `+0x4B0/+0x4B4/+0x4B8/+0x4BC`: secondary box offset X/Y and width/height
- `+0x570/+0x574/+0x578/+0x57C`: attack/hit box offset X/Y and width/height
- `+0x590..+0x598`: vector/list of derived/sorted box descriptors rebuilt by `0x4A0A30`

Contacts and collision state:

- `+0x2A8`: collision/gravity suppression flag used by `Entity_ApplyGravity`
- `+0x2AC`: current floor tile pointer from floor check
- `+0x2B0`: in-air flag
- `+0x2B4`: floor/landed tile pointer copy
- `+0x2C4`: air-time counter
- `+0x2D4`: transition/form timer set by `0x4A14B0`
- `+0x2D8/+0x2DC/+0x2E0/+0x2E4`: left/right/top/bottom contact flags used by restore/collision paths
- `+0x32C`: standing parent/platform pointer or flag; suppresses gravity when nonzero

Movement helpers:

- `0x4A1200`: MoveX, `speed_x/impact_x`
- `0x4A13B0`: MoveY, `speed_y/impact_y`
- `0x49E920`: scalar decay toward zero by `entity+0xD0`
- `0x49FAA0`: converts a scalar by `value / g_friction_constant / 60`
- `0x49E820`: clamp/add `speed_y` to `y_min`, then world-bound check
- `0x49E1E0`: world-bound clamp/status, not tile collision
- `0x46A4B0`: decays `impact_x` at `+0x288` toward zero by `1/g_friction_constant` after movement/collision handling
- `0x46A560`: decays `impact_y` at `+0x290` toward zero by `1/g_friction_constant`; calls `Entity_ZeroVelocity` when crossing zero

Collider/tile helpers:

- `0x466C10`: `Entity_SetCollisionBox(entity, anchor_mode, width, height)` writes collision box offsets/sizes at `+0x470..+0x48C`
- `0x4A14B0`: entity form/state setter; recomputes sprite box, main collision box, hit/hurt boxes, and calls `0x4A0A30`
- `0x4A0A30`: rebuilds/sorts the entity's box descriptor list from the main collision, secondary, and attack/hit box dimensions
- `0x40CB10`: samples tile collision along an edge and returns the tile index with highest collision attribute
- `0x40D740`: floor check using bottom edge, sets `+0x2AC/+0x2B4`, updates `in_air/air_time`, and calls `Entity_OnLand`
- `0x40D670`: ceiling check using top edge and calls `Entity_OnHitCeiling`

## Entity creation/init path

- `0x4A0580`: `PlayerEntity_Init` / constructor. Calls `Entity_BaseInit`, initializes player-specific vectors/lists and collision-box descriptor storage around `+0x500..+0x5B8`, then installs player vtable `off_56B7B4`.
- `0x4661B0`: `Entity_BaseInit` / common constructor. Installs base vtable `off_56B5B8` and initializes the shared vectors, actor descriptor state, command/action storage, variables, contact state, and box state.
- `0x49B200`: `Entity_InitFromActorDesc`. Copies actor descriptor data into an entity and sets sprite/collision dimensions, command/action/state fields, text/effect vectors, and animation state. This is the shared actor descriptor copy path, not a player-only path.
- `0x4A0870`: `Entity_SetWorldPosition` / player spawn init. Copies actor descriptor state, sets block/collision boxes, stores the input device pointer at `+0x4D0`, calls the form/state setter `0x4A14B0`, writes the initial speed scalar `4/g_friction_constant` at `+0x4E0`, clears runtime movement flags, and rebuilds box descriptors.
- `StageScene_LoadStage` reaches the player/entity setup through callees including `PlayerEntity_Init`, `Entity_SetWorldPosition`, `Entity_InitFromActorDesc`, `StageScene_SpawnActor`, and `StageScene_SpawnBlock`.
